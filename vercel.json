{
    "builds" : [
        {
            "src": "main.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        { "handle": "filesystem" },
        { "src": "/(.*)", "dest": "/" }
    ]
}